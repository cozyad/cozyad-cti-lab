# Splunk Universal Forwarder outputs.
# Replace SPLUNK_INDEXER_IP with the internal VPC IP of VM1 before applying
# manually. The bootstrap writes this dynamically from instance metadata.

[tcpout]
defaultGroup = cti_indexers

[tcpout:cti_indexers]
server = SPLUNK_INDEXER_IP:9997
useACK = true
